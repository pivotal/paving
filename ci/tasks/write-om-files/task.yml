---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: cfinfrastructure/releng-tools

inputs:
- name: env-state
- name: paving-ci

outputs:
- name: env
- name: config

params:
  OM_USERNAME:
  OM_PASSWORD:

  AUTH_CONFIG_FILE:
  ENV_YML:

run:
  path: paving-ci/ci/tasks/write-env-yml/task
